# ÉDITER CE FICHIER pour ajouter/modifier une dépendance.
# PUIS recompiler le lock AVEC PYTHON 3.11 (version du runtime Docker) :
#   docker run --rm -v "$(pwd):/work" -w /work python:3.11-slim \
#     bash -c "pip install pip-tools && pip-compile requirements.txt --output-file requirements.lock --generate-hashes"
# Le Dockerfile installe depuis requirements.lock, pas requirements.txt.
# NE PAS éditer requirements.lock manuellement.

fastapi>=0.109.0
uvicorn>=0.27.0
presidio-analyzer>=2.2.362
presidio-anonymizer>=2.2.362
spacy==3.7.2
redis>=5.0.1
cryptography>=46.0.6
PyJWT>=2.8.0,<3
passlib>=1.7.4
python-multipart>=0.0.29
httpx>=0.26.0
anthropic>=0.25.0
openai>=1.27.0
PyMuPDF>=1.26.7
python-docx>=1.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
defusedxml>=0.7.1
langdetect>=1.0.9
pydantic>=2.6.0
pydantic-settings>=2.1.0
email-validator>=2.1.0
slowapi>=0.1.9
aiofiles>=23.2.1
litellm>=1.83.7,<1.83.14
reportlab>=4.0.0
pytesseract>=0.3.10
openpyxl>=3.1.0
Pillow>=12.2.0
xlrd>=2.0.1
pytest>=9.0.3
pytest-asyncio>=1.3.0
fakeredis>=2.21.0
python-magic>=0.4.27
PyYAML>=6.0.0

# Pinning securite (override transitives — CVE 2026)
cryptography>=46.0.6
filelock>=3.20.3
pillow>=12.2.0
pymupdf>=1.26.7
# DECISIONS SECURITE TRACEES (litellm epingle ses deps en == strict) :
# - aiohttp==3.13.3 (impose par litellm) : CVE-2026-22815/34516/34519/34520 neutralisees
#   via DISABLE_AIOHTTP_TRANSPORT=True (litellm utilise httpx==0.28.1 a la place).
# - python-dotenv==1.0.1 (impose par litellm) : CVE-2026-28684 non exploitable
#   (set_key/unset_key non utilises, app en lecture seule du .env).
# - litellm CVE-2026-40217 (fixee 1.83.10) : risque accepte, 1.83.10+ garde les memes
#   pins problematiques. A revoir quand litellm assouplira ses contraintes.
# Revue requise a chaque montee de version litellm.
python-multipart>=0.0.29
requests>=2.33.0
urllib3>=2.7.0
