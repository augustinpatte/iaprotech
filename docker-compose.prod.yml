version: "3.9"

# ============================================================
# Privacy Proxy — Docker Compose PRODUCTION
# Domaine : iaprotech.com / api.iaprotech.com
# ============================================================

services:
  # ----------------------------------------------------------
  # Backend — FastAPI + Presidio
  # ----------------------------------------------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: always
    deploy:
      resources:
        limits:
          memory: 3G
          cpus: "2"
    env_file: .env.prod
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
    volumes:
      - backend_logs:/app/logs
    networks:
      - proxy_net
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "healthcheck.py"]
      interval: 30s
      timeout: 10s
      start_period: 180s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"

  # ----------------------------------------------------------
  # Frontend — React (Vite build statique servi par Nginx interne)
  # VITE_API_URL est injecté au moment du build (ARG Docker)
  # ----------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=https://api.iaprotech.com
    restart: always
    networks:
      - proxy_net
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"

  # ----------------------------------------------------------
  # Redis — vault PII chiffré, AOF activé, refus des écritures si plein
  # ----------------------------------------------------------
  redis:
    image: redis:7.4.8-alpine
    restart: always
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD}
      --appendonly yes
      --appendfsync everysec
      --maxmemory 1gb
      --maxmemory-policy noeviction
      --rename-command FLUSHDB ""
      --rename-command FLUSHALL ""
      --rename-command CONFIG ""
      --rename-command DEBUG ""
      --rename-command KEYS ""
    volumes:
      - redis_data:/data
    networks:
      - proxy_net
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"

  # ----------------------------------------------------------
  # Nginx — reverse proxy public (HTTP→HTTPS, SSL Let's Encrypt)
  # ----------------------------------------------------------
  nginx:
    image: nginx:1.29.8-alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.prod.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - nginx_cache:/var/cache/nginx
    networks:
      - proxy_net
    depends_on:
      - frontend
      - backend
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "10"

# ----------------------------------------------------------
# Réseau privé — backend et redis non exposés directement
# ----------------------------------------------------------
networks:
  proxy_net:
    driver: bridge

# ----------------------------------------------------------
# Volumes persistants
# ----------------------------------------------------------
volumes:
  backend_logs:
    driver: local
  redis_data:
  nginx_cache:
